# 실험 기록 — 상세

[README](README.md) 의 결론이 어떤 측정에서 나왔는지에 대한 전체 기록이다.
시간 순으로 정리했으며, **틀린 판단과 그 정정도 그대로 남겨 두었다.**

측정 조건·지표 정의는 [README 의 지표 읽는 법](README.md#-검증-결과) 참조.

---

### A. 합성 샘플 (12건) — 목표 달성

| 지표 | OCR 원본 | **후처리 적용** | 목표 | 달성 |
|------|----------|-----------------|------|------|
| 평균 CER (문자 오류율) | 0.072 | **0.029** | 0.15 이하 | ✅ |
| 공백 무시 일치율 | 3/12 (25%) | **10/12 (83%)** | 80% 이상 | ✅ |
| 이미지 1장 처리 시간 | — | **8.55초** | 5초 이하 | ❌ (CPU 한계) |

### 기존 방식과의 비교

| 문항 | 기존: 재타이핑 (추정) | PoC: 촬영 + OCR | 결과 |
|------|----------------------|-----------------|------|
| 51·52번 (한 문장) | 약 20초 | 약 9초 | 개선되나 체감 폭 작음 |
| 54번 (600~700자) | **약 5~8분** | **약 9초** | **압도적 개선** |

→ 분량이 큰 문항일수록 효용이 커진다. **54번이 이 PoC의 핵심 수혜 구간.**

### 🔑 핵심 발견 — 오류가 한 곳에 몰려 있었다

OCR 원본의 실패 9건을 분석하니 **전부 종성(받침) 오인식**이었다.

| 오류 유형 | 사례 | 건수 |
|-----------|------|------|
| 종성 ㅂ → ㅁ | 바**랍**니다 → 바**람**니다 | 3 |
| 조사 을 → 올 | 동물**을** → 동물**올** | 3 |
| 종성 ㄱ → ㅋ | 감사하**겠**습니다 → 감사하**켓**습니다 | 1 |
| 종성 ㅆ → ㅅ | 나타**났**다 → 나타**낫**다 | 1 |
| 기타 (를→름, 뿐→분) | 공기**를** → 공기**름** | 1 |

평균 CER은 0.072로 이미 낮았다(글자의 93%가 정확). **문장마다 딱 한 글자, 그것도 받침만 틀려서**
완전일치율만 25%로 주저앉은 것이다. 즉 **모델을 바꿀 문제가 아니라 후처리로 풀 문제**였다.

### 해결 — 한국어 문법 기반 후처리

관측된 오답을 보고 짜맞춘 규칙이 아니라, **한국어에 존재하지 않는 형태**를 근거로 교정한다.

| 규칙 | 근거 |
|------|------|
| 종성 ㅁ + `니다` → ㅂ + `니다` | 한국어에 `-ㅁ니다` 어미는 없다 |
| 어절 끝 `올` → `을` | `올`은 목적격 조사가 아니다 |
| `켓습니다` → `겠습니다` | 한국어에 `-켓습니다` 어미는 없다 |

**결과: 일치율 25% → 83%, CER 0.072 → 0.029** (모델 변경·재학습 없음)

과교정(멀쩡한 글자를 망가뜨리는 것)을 막기 위해 `selfcheck` 에 방어 테스트를 넣었다 —
`감사합니다`·`없습니다`·`서울에`·`올해는` 이 변형되지 않는지 검증한다.

### ❌ 남은 실패 사례 (정직하게)

| 정답 | 결과 | 왜 안 고쳤나 |
|------|------|--------------|
| 건강에 도움이 되는 것으로 나타**났**다 | 나타**낫**다 | `낫다`(병이 낫다)가 실제 단어라 일괄 교정 시 오히려 오류 발생 |
| **공기를 통해서뿐만** 아니라 | **공기름 통해 서분만** | 어절 끝 `름`→`를` 교정은 `이름`·`여름`·`구름` 을 망가뜨림 |

→ 두 경우 모두 **문맥 없이 규칙만으로는 안전하게 교정 불가.** 사전·언어모델 기반 후처리가 필요한 구간이다.

---

### B. 실제 손글씨 (53행) — easyocr 목표 미달

> 아래는 **easyocr 기준** 결과다. 이후 [C. 엔진 교체 실험](#c-엔진-교체-실험-colab-t4-gpu)에서 결론이 일부 뒤집힌다.

합성 샘플만으로는 실제 성능을 알 수 없어, **공개된 실제 한글 손글씨 사진**([입력 샘플 B](#b-실제-손글씨--레포에-미포함-53행--필체-5종))으로 재측정했다.

측정 환경: **Colab T4** · 리포트: [results/report_real.md](results/report_real.md)

| 지표 | OCR 원본 | 후처리 적용 | 목표 | 달성 |
|------|----------|-------------|------|------|
| 평균 CER | 0.196 | **0.196** | 0.15 이하 | ❌ |
| 공백 무시 일치율 | 1/53 (2%) | **1/53 (2%)** | 80% 이상 | ❌ |

**한 건도 정확히 읽어내지 못했다.**

```
정답  기출문제를 다 푼 뒤 선생님이 기말고사를 볼 때 볼펜으로 풀지 말고 연필로 연하게
결과  기술 눈제출 다 표 뒤 선심님이 기말고사큼 볼 때 불편로 풀지 말고 연필로 연하게

정답  선생님이 알려 주셨는데 기출문제을 학원에 주면 안 되지않을까? 그래서
결과  선생님이 알려 주 _눈데 기출문제을 학원에 주면 안 되지않 올 까? 그러서
```

#### 교란 변수 통제 — 어노테이션 박스는 원인이 아니었다

데모 이미지에는 초록색 어노테이션 박스가 인쇄되어 글자와 겹쳐 있었다.
"손글씨가 어려운 것"과 "박스가 방해한 것"을 분리하기 위해 박스를 배경색으로 제거하고 재측정했다.

| 조건 | 평균 CER (원본) |
|------|-----------------|
| 초록 박스 있음 | 0.229 |
| 초록 박스 제거 | 0.226 |

→ **차이 없음.** 성능 저하의 원인은 박스가 아니라 **손글씨 자체**임이 확인됐다.

#### 왜 후처리가 실제 데이터에서는 안 통했나

| | 합성 샘플의 오류 | 실제 손글씨의 오류 |
|---|---|---|
| 오류 위치 | **종성(받침)에만 집중** | 초성·중성·종성 **전방위** |
| 어절 경계 | 대체로 유지 | **과분할** (`기출문제를` → `기 출문 제틀`) |
| 대표 사례 | 바랍니다→바람니다 | 선생님→선심님, 복제→목제, 위반→우반 |
| 규칙 3개로 커버 | ✅ 25%→83% | ❌ 0%→0% |

합성 데이터의 오류는 "받침 하나"라는 **좁고 규칙적인** 형태였기에 문법 규칙으로 잡혔다.
실제 손글씨는 오류가 글자 전체에 퍼져 있어 **규칙 기반 후처리의 사정거리를 벗어난다.**

#### 📌 이 실험이 증명한 것

> **합성 샘플로 측정한 83%는 실제 성능을 심각하게 과대평가했다.**
> easyocr 의 실제 값은 0%였다. "합성 결과는 성능의 상한선"이라는 가정이 **실측으로 확인**됐다.

---

### C. 엔진 교체 실험 (Colab T4 GPU)

"모델이 손글씨를 학습하지 않은 것"이 원인이라면 **다른 모델은 다를 수 있다**는 가설을 검증했다.
데이터([입력 샘플 B](#b-실제-손글씨--레포에-미포함-11건))·후처리·CER 계산·정답 라벨은 **완전히 동일**하고 엔진만 교체했다.

| 엔진 | 평균 CER | 일치율 | 리포트 | 비고 |
|------|----------|--------|--------|------|
| easyocr | 0.189 | 1/53 | [report_real.md](results/report_real.md) | 인쇄체 중심 학습 |
| **surya 0.17.1** | **0.071** | 9/53 | [report_real_surya.md](results/report_real_surya.md) | 문서·다국어 학습, 어절 구조 유지 |

(CER 은 10자 미만 단편 2건을 제외한 51행 기준)

**오류의 성격이 다르다:**

| | easyocr | surya |
|---|---|---|
| 어절 경계 | **붕괴** (`기출문제를`→`기 출문 제틀`) | **유지** (`기출문제를` ✅) |
| 오류 형태 | 자모 전방위 붕괴 | **드문 글자 오치환** (`기출`→`기童`, `선생님`→`선범님`) |
| 후처리 효과 | +0%p | +0%p (오류 유형이 달라 규칙이 안 맞음) |

surya 는 문장 구조를 유지한 채 **개별 글자 몇 개만 틀린다.** 사람이 읽고 고치기 훨씬 쉬운 형태다.

#### 실행 중 만난 문제 (재현 시 참고)

| 문제 | 해결 |
|------|------|
| surya 0.22.1 은 모든 백엔드가 외부 서버·컨테이너 기반 (vLLM=Docker, openai_client=API 서버) | Colab 에서 Docker 불가 → **0.17.1 로 다운그레이드** (인프로세스 torch 추론) |
| surya 0.17.1 + transformers 5.x → `SuryaDecoderConfig has no attribute pad_token_id` | `transformers<5` 로 고정 |

```bash
pip install "surya-ocr==0.17.1" "transformers<5"
```

#### 📌 결론 수정

앞선 "가설 기각"은 **easyocr 에 한정된 결론**이었다. 엔진을 바꾸자 **CER 목표(0.15)는 달성**됐다.

도메인 관점의 함의:
- **51/52번 자동채점** (정답 문자열 비교): 일치율 9% 로 **여전히 불가**
- **B 전략(초벌 전사 + 사용자 확인)**: CER 0.071 = 글자의 **93% 정확**, 어절 구조도 유지 → **실현 가능성이 크게 올라감**
  (단, 필체에 따라 0.065~0.157 로 흔들리므로 최악 필체 기준으로 설계해야 한다)
- **문법 자동 첨삭**: OCR 오류를 문법 오류로 오탐할 위험은 남아 있음

---

### D. 입력 단위 실험 — 전처리가 오히려 해로웠다

**질문**: 실제 학습자는 답안지를 통째로 촬영한다. 우리가 행 단위로 잘라 넣는 전처리는 정확도에 기여할까?

정답·엔진·후처리·지표를 **완전히 동일하게** 두고 **입력 단위만** 바꿔 측정했다.
(전면 이미지도 행 크롭과 똑같이 어노테이션 박스를 제거해 공정성을 맞췄다.)

| 항목 | 행별 분할 | **페이지 통째** |
|------|-----------|-----------------|
| API 호출 수 | 53회 | **5회** (10.6배 절감) |
| 평균 CER | 0.073 | **0.037** |
| CER (띄어쓰기 무시) | 0.039 | **0.021** |

| 페이지 | 행별 CER | 페이지 CER | 차이 |
|--------|----------|------------|------|
| 002 | 0.092 | 0.040 | **−0.052** |
| 007 | 0.078 | 0.045 | −0.033 |
| 008 | 0.062 | 0.026 | −0.035 |
| 009 | 0.054 | 0.038 | −0.016 |
| 010 | 0.081 | 0.033 | −0.047 |

**5개 페이지 전부에서 페이지 통째가 더 정확했다.**

#### 왜 전처리가 해로웠나 (추정)

- **엔진의 레이아웃 분석을 우회했다.** CLOVA 는 자체 행 검출을 갖고 있고, 그쪽이 우리의
  가로 투영 방식보다 정교하다. 잘라서 넣는 순간 그 기능을 못 쓰게 된다
- **극단적 가로세로비.** 행 크롭은 3024×120 으로 일반 문서와 동떨어진 형태다
- **문맥 상실.** 페이지 전체를 보면 글자 크기·기준선·필체를 문서 단위로 보정할 수 있다
- **획 잘림.** 크롭 패딩(12px)이 위아래 획을 자를 수 있다

#### 세 엔진 모두 재측정 — 페이지 이점은 **CLOVA 고유**였다

같은 실험을 easyocr·surya 에도 적용했다. 결과는 예상과 달랐다.

| 엔진 | 행별 분할 | 페이지 통째 | 페이지 승 | 판정 |
|------|-----------|-------------|-----------|------|
| easyocr | **0.184** | 0.191 | 1/5 | 페이지가 약간 나쁨 |
| surya | **0.070** | 0.083 | 2/5 | 페이지가 나쁨 |
| **CLOVA** | 0.073 | **0.037** | **5/5** | **페이지가 훨씬 좋음** |

> **입력 형태의 최적값은 엔진마다 다르다.**
> CLOVA 는 문서 전체의 레이아웃을 분석하도록 만들어진 상용 문서 OCR 이라 페이지 입력에서 강하다.
> 반면 easyocr·surya 는 우리가 명시적으로 잘라 준 행을 더 잘 처리한다.

#### 📌 배운 것

1. **전처리가 항상 도움이 되는 것은 아니다** — CLOVA 에서는 우리 전처리가 정확도를 절반으로 깎았다.
2. **그렇다고 전처리가 항상 해로운 것도 아니다** — easyocr·surya 에서는 오히려 전처리가 도움이 됐다.
3. → **엔진을 바꾸면 파이프라인도 함께 다시 재야 한다.** 엔진과 전처리는 독립 변수가 아니다.

#### 최종 순위 (각 엔진의 최적 입력 기준)

| 순위 | 엔진 | 최적 입력 | CER |
|------|------|-----------|-----|
| 🥇 | **CLOVA OCR** | 페이지 통째 | **0.037** |
| 🥈 | surya 0.17.1 | 행별 분할 | 0.070 |
| 🥉 | easyocr | 행별 분할 | 0.184 |

**엔진 순위 자체는 입력 형태와 무관하게 동일했다**(CLOVA > surya > easyocr).
다만 최적 입력을 쓰면 1·2위 격차가 1.9배로 벌어진다.

제품 관점에서도 CLOVA 방식이 유리하다. 페이지 입력은 **실사용 흐름(답안지 한 장 촬영) 그 자체**라
행 분할 전처리를 파이프라인에서 들어낼 수 있고, 호출 비용도 10배 싸다.

#### 호출을 더 줄일 수 있을까

페이지 단위로 5회까지 줄였으니, 더 줄일 수 있는지 두 가지를 실측했다.

| 방법 | 결과 | 원인 |
|------|------|------|
| **배치** (`images[]` 배열에 5장) | ❌ **불가 확정** | API 가 명시적으로 거부: `images=size must be between 0 and 1` — **요청당 1장만 허용** |
| **병합** (5장을 1장으로 이어붙임) | ⚠️ 원본 크기에서 실패 | 3024×20160 / base64 17.6MB → HTTP 200 이지만 **인식 결과 0건** |

병합 실패는 특히 위험한 형태였다. **오류가 아니라 빈 결과로 돌아왔다.**
운영 환경이라면 "글자가 없는 이미지"와 구분되지 않아 조용히 잘못된 채점을 낼 수 있다.
→ 응답의 `inferResult` 를 확인해 처리 실패를 예외로 드러내도록 코드를 고쳤다.

단, 이는 **"병합이 불가능하다"가 아니라 "이 크기에서 실패한다"** 는 뜻이다.
축소율을 바꿔가며 재측정한 결과, **병합은 가능했다.**

#### API 제한의 정체 — 바이트가 아니라 해상도

오류 본문을 확인하니 원인이 명확했다.

```
The image resolution limit has been exceeded.
adjust the image to a size between 10 px and 8000 px in width and height
```

**가로·세로 각각 최대 8000px.** 용량 제한이 아니었다. 따라서 병합본을 8000px 아래로만 만들면 된다.

#### 여백 제거가 병합을 가능하게 했다

페이지 아래쪽 빈 공간이 전체 높이의 절반가량이다. 글자 영역만 잘라내면:

| | 높이 | 8000px 제한 |
|---|------|-------------|
| 여백 포함 | 20160px | 40% 로 줄여야 통과 |
| **여백 제거** | **10199px (51%)** | **70% 로 통과** |

같은 목표 크기에 **더 큰 배율**을 쓸 수 있어 글자 해상도가 그만큼 보존된다.
검출 비용은 장당 약 2초로 무시할 수준이다.
(다만 효과는 답안 분량에 따라 다르다 — 009 는 37% 로 줄지만 010 은 74% 에 그친다.)

#### 결과: 호출 1회로 정확도 유지

| 배율 | 크기 | CER | 비고 |
|------|------|-----|------|
| 100% | 3024×10199 | ❌ | 8000px 초과 |
| 70% | 2116×7139 | 0.042 | |
| **50%** | **1512×5099** | **0.038** | **최적** |
| 35% | 1058×3569 | 0.039 | |
| 25% | 756×2549 | 0.045 | |
| 15% | 453×1529 | 0.051 | |

| 방식 | 호출 | CER |
|------|------|-----|
| 행별 분할 | 53회 | 0.073 |
| 페이지별 | 5회 | 0.037 |
| **병합 1장 (50%)** | **1회** | **0.038** |

**호출을 5분의 1로 줄이면서 정확도는 사실상 동일하다**(0.037 vs 0.038).

#### 🔍 예상 밖의 발견 — 해상도 여유가 매우 크다

단일 페이지를 배율별로 재보니, **줄여도 정확도가 떨어지지 않았다.**

| 배율 | 크기 | CER |
|------|------|-----|
| 100% | 3024×2214 | 0.057 |
| 50% | 1512×1107 | 0.050 |
| 25% | 756×553 | 0.050 |
| **15%** | **453×332** | **0.042** ← 원본보다 좋다 |

가로 **453px** 에서도 CER 0.042 다. 축소가 종이 질감·촬영 노이즈를 평활화해
오히려 인식에 유리하게 작용한 것으로 보인다.

**제품 함의**: 사용자가 고해상도 원본을 올릴 필요가 없다.
업로드 용량을 1.9MB → 0.1MB 로 줄여도 정확도가 유지되므로,
**클라이언트에서 축소 후 전송**하는 것이 대역폭·속도 면에서 유리하다.

#### 여백 제거는 단일 페이지에 영향이 없다 (5페이지 재측정)

002 한 장에서 여백 제거가 불리해 보여(0.040 → 0.057) 5페이지를 **같은 실행에서** 두 조건으로 다시 쟀다.

| 페이지 | 여백 포함 | 여백 제거 | 차이 |
|--------|-----------|-----------|------|
| 002 | 0.040 | 0.057 | **+0.017** |
| 007 | 0.045 | 0.043 | −0.003 |
| 008 | 0.026 | 0.024 | −0.003 |
| 009 | 0.038 | 0.041 | +0.003 |
| 010 | 0.033 | 0.039 | +0.005 |
| **평균** | **0.037** | **0.041** | +0.004 |

평균만 보면 제거가 불리하지만, **002 를 빼면 나머지 4 장은 전부 ±0.005 이내**로 차이가 없다.
방향도 2:2 로 갈린다. 평균을 끌어올린 것은 002 한 장이다.

> **정직한 결론: 여백 제거는 단일 페이지 인식률에 유의미한 영향이 없다.**
> 처음의 "불리하다"는 관찰은 **표본 1장에 기댄 착시**였다.

**전처리 정책**

| 상황 | 여백 제거 | 이유 |
|------|-----------|------|
| 병합 (여러 장 → 1장) | ✅ **필수** | 8000px 제한을 통과해야 한다 |
| 단일 페이지 | ❌ **불필요** | 정확도 이득이 없고 장당 약 2초만 더 든다 |

---

### E. 오류 유형 분석 — 왜 틀리는가

남은 병목인 **글자 오인식 36행**을 자모 단위로 분해했다 (API 호출 0회, [analyze_errors.py](analyze_errors.py)).
상세: [results/error_analysis.md](results/error_analysis.md)

#### 오류의 절반 가까이가 "글자 누락"이다

| 종류 | 글자 수 |
|------|---------|
| 치환 (다른 글자로 읽음) | 33 |
| **삭제 (글자를 아예 놓침)** | **28** |

치환만 신경 쓰기 쉬운데, **삭제가 거의 같은 비중**이다. 후처리로는 손댈 수 없는 유형이다.

#### 치환 오류의 52%는 초성에서 난다

| 위치 | 비율 |
|------|------|
| **초성** | **52%** |
| 종성 | 14% |
| 중성 | 7% |
| 2개 이상 동시 | 17% |

#### 획을 잃는 방향으로 편향돼 있다

| 혼동 | 횟수 | 성격 |
|------|------|------|
| **ㅊ → ㅈ** | **7** | 위 점 획을 놓침 |
| ㅇ → ㅁ (종성) | 3 | 둥근 획이 각지게 |
| ㅎ → ㅇ | 2 | 위 획을 놓침 |
| ㅂ → ㅁ | 2 | 세로획을 놓침 |

`출 → 줄` 하나만 4건이다. 오류가 무작위가 아니라 **"획이 사라지는 방향"으로 일관**된다.
빠르게 쓴 손글씨에서 획이 생략·연결되는 특성과 맞물린 결과로 보인다.

#### 📌 그런데 부분 교정으로는 일치율을 못 올린다

가장 잦은 `ㅊ → ㅈ` 는 오류 행 36건 중 **10건**에 등장한다. 꽤 큰 비중이다.
그래서 이것만 고치면 얼마나 살아나는지 계산해 봤다.

> **완전 복구되는 행: 0건.**

이유는 명확하다. 글자 오류 **61건이 36행에 분포**해 **행당 평균 1.7건**이다.
대부분의 오류 행에는 원인이 둘 이상이라, 한 가지를 완벽히 고쳐도 그 행은 여전히 틀린다.

**이것이 후처리가 실제 데이터에서 0%p 였던 근본 이유다.**
CER 은 조금 내려가겠지만 **완전일치는 한 행의 모든 오류를 동시에 고쳐야** 달성된다.

→ 일치율을 올리려면 후처리가 아니라 **인식률 자체**를 올려야 한다(파인튜닝·더 나은 모델).
또는 제품 전략을 **완전 자동이 아닌 "사람 확인"** 으로 두어야 한다.

---
### F. 비전 LLM — 가장 정확하지만 학습자 오류를 지운다

전통 OCR 엔진이 아니라 **이미지를 이해하는 언어모델**로 읽으면 어떨까.
API 키 없이 검증하기 위해, 이 대화를 모르는 **별도 컨텍스트의 에이전트**에게
라벨 없는 폴더의 이미지 10장만 주고 전사시켰다(블라인드).

#### 결과 — 정확도는 압도적

| 엔진 | CER | 공백무시 일치 |
|------|-----|---------------|
| easyocr | 0.189 | 2% |
| surya | 0.071 | 17% |
| CLOVA | 0.075 | 32% |
| **비전 LLM** | **0.010~0.016** | **60~80%** |

같은 10행에서 CLOVA 가 0.063 일 때 LLM 은 0.010 이었다. **6배 차이다.**

#### 왜 잘 읽는가 — 구조가 다르다

```
OCR 엔진 : 이미지 → [글자 위치 검출] → 조각별 분류 → 텍스트
                                        └ 조각 하나만 보고 판단

비전 LLM : 이미지 → [토큰 변환] → 언어모델이 다음 글자를 생성
                                  └ 문장 전체 + 어휘 지식으로 판단
```

| 사례 | CLOVA | LLM | 이유 |
|------|-------|-----|------|
| `기출문제를` | 기줄문 제를 | ✅ | `ㅊ`/`ㅈ` 은 조각만 보면 유사. LLM 은 "기줄문제"가 없는 말임을 앎 |
| `시작되었습니다` | 시작되 있습니다 | ✅ | 뭉갠 자리를 어미 패턴으로 복원 |
| `다가올` | 다가을 | ✅ | `올`/`을` 구분에 문법 지식 필요 |

> **OCR: "이 획이 어떤 글자에 가장 가까운가"** (형태 매칭)
> **LLM: "이 맥락에서 어떤 글자가 와야 말이 되는가"** (의미 추론)

#### ⚠️ 그런데 같은 능력이 단점이 된다

우리는 **쓰기 시험을 채점**한다. 학습자가 낸 오류를 그대로 읽어야 한다.

| 조건 | 오류 보존 | 지워진 것 |
|------|-----------|-----------|
| A. 자연 지시 | 7/9 | `오라하지`→오라 하지, `학원수 업이`→학원 수업이 |
| B. "고치지 마세요" 지시 | 7/9 | `발전을위해서`→발전물위해서 |
| C. `[?]` 표시 지시 | **8/9** | `만들었고.`→만들었고, |

프롬프트로 어느 정도 억제되지만 **완전히 막지는 못한다.**

#### 📌 정답이 틀렸던 사례 — 사람 판독의 한계

`007_line02` 를 `폼출할`로 전사했으나, 에이전트가 4~14배 확대해 `표출할`이라고 지적했다.
직접 확대 확인한 결과 **ㅁ 받침이 없고 ㅍ 구조가 뚜렷했다. 우리 정답이 틀렸다.**

> 즉 "LLM 이 학습자 오류를 지웠다"고 본 사례 하나는 **필자의 오독**이었고,
> **LLM 이 사람보다 정확했다.** 한계 6번(정답은 사람 판독)이 실제로 데이터에 영향을 준 사례다.

#### 하이브리드 시도 1 — LLM 이 표시하고 OCR 이 해소 → 실패

`[?]` 지점을 CLOVA 값으로 대체해 보았다.

| | CER | 오류 보존 |
|---|-----|-----------|
| C-1. 표시만 (마크 제거) | **0.010** | **8/9** |
| C-2. `[?]` 를 CLOVA 로 대체 | 0.014 | 7/9 |

**오히려 나빠졌다.** LLM 이 헷갈리는 지점은 CLOVA 도 헷갈린다.
잘 읽은 결과를 못 읽는 값으로 덮은 셈이다.

---

### G. 앙상블 — 정답 없이 불확실 지점을 찾는다

엔진마다 다른 실수를 하므로, **두 엔진이 다르게 읽은 곳 = 애매한 곳**이라는 가설.
새 호출 없이 기존 CLOVA·surya·easyocr 결과로 검증했다.

#### 불일치는 강력한 오류 신호다

어절을 위치 정렬해 대조한 결과 (529 어절):

| | 어절 수 | 정확도 |
|---|---------|--------|
| 두 엔진 **합의** | 315 | **96%** |
| 두 엔진 **불일치** | 214 | **33%** |

전체 오류 157개 중 **143개(재현율 91%)** 를 불일치가 잡아냈다.

실제 예 (`009_line04`):

```
CLOVA : 쉬는 시간이 끝났고 우리는 교실로 돌아왔다. 종례후 우리는 다 함께 학원으로
surya : 위는 시간이 끝났고 우리는 교실로 돌아왔다. 종례후 우리는 다 항기게 학원으로
        ⚠️                                                    ⚠️
```

두 엔진이 다르게 읽은 2곳이 정확히 어려운 지점이었다.

#### 표시량 조절 — 엔진 수와 기준에 따른 트레이드오프

| 기준 | 표시량 | 미표시 정확도 | 오류 재현율 |
|------|--------|---------------|-------------|
| 3엔진: 하나라도 불일치 | 65% | **98%** | **98%** |
| 2엔진: 불일치 | 40% | 96% | 91% |
| 3엔진: 둘 다 불일치 | **28%** | 88% | 71% |

**"미표시 정확도"가 핵심이다.** 표시되지 않은 부분은 사용자가 넘어가므로,
그곳이 얼마나 믿을 만한지가 설계 기준이 된다.

#### CLOVA 는 결정적이다 (검증함)

앙상블·재현성 논의는 "OCR 은 항상 같은 결과를 낸다"는 전제 위에 서 있었으나
확인한 적이 없었다. 같은 이미지 10건을 재호출해 대조했다.

> **10/10 완전 동일.** 같은 곳에 항상 같은 표시가 붙는다.

⚠️ 단, 10건·1회·같은 날 기준이다. 서비스가 모델을 갱신하면 달라질 수 있다.

#### ⚠️ 불일치의 63%는 판별 불가

"불일치 = 두 후보 중 하나가 정답"이 아니었다.

```
불일치 163개
├─  61개 (37%) — A/B 중 하나가 정답 → 판별 의미 있음
└─ 102개 (63%) — 어절 분할 어긋남 / 둘 다 오답 → 선택으로 안 풀림
```

앙상블의 산출물은 **"여기가 위험하다"** 까지이고, **"둘 중 무엇이 맞다"** 는 주지 못한다.

---

### H. 앙상블 + LLM 판별 — 오류를 보존하면서 해소한다

F 에서 LLM 은 **전사**시키면 학습자 오류를 고쳐버렸다.
그렇다면 **객관식 판별**만 시키면 어떨까 — 새 텍스트를 생성할 통로를 막는 것이다.

> "이 이미지를 CLOVA 는 `A`, surya 는 `B` 로 읽었다. **어느 쪽이 이미지와 일치하는가?**
>  새로 쓰지 말고 둘 중 하나만 고르라. 맞춤법이 아니라 **쓰인 획**으로 판단하라."

G 에서 걸러낸 깨끗한 1:1 케이스 12건으로 블라인드 테스트했다.

| 결과 | 값 |
|------|-----|
| 판별 정확도 | **11/12 (92%)** |
| 학습자 오류 보존 | ✅ |

#### 결정적 사례 — case04

| 이미지에 쓰인 것 | 맞춤법상 옳은 것 | LLM 선택 |
|------------------|------------------|----------|
| **`이였다`** | `이었다` | ✅ **`이였다`** |

에이전트 근거: *"모음에 가로획이 두 개(ㅕ)라 학습자가 `이였다`로 썼다 — 틀린 표기가 지면에 있는 그대로다"*

**전사시킬 때는 고치던 것을, 판별시키니 고치지 않았다.**

| | 전사 (생성) | 판별 (선택) |
|---|-------------|-------------|
| 출력 | 자유 텍스트 | A 또는 B |
| 고쳐 쓸 여지 | 있음 | **없음** |
| 판단 기준 | "말이 되게" | **"획이 어느 쪽인가"** |

#### 유일한 오답

`case07` (`하고` vs `"하고`) — 따옴표가 어느 어절에 속하는지의 문제였다.
에이전트가 픽셀 간격까지 측정했으나 틀렸다. **어절 경계 판정은 획 대조로 풀리지 않는 종류**다.

#### 한계

- 판별 가능한 것은 불일치의 **37%** 뿐이다. 나머지는 여전히 사람이 원본을 봐야 한다
- 에이전트가 **9분, 도구 64회**를 썼다(확대·픽셀 측정 반복). 프로덕션에서는 이 시간을 쓸 수 없어 정확도가 낮아질 것이다
- 12건 표본이며, 케이스 선정을 필자가 했다
