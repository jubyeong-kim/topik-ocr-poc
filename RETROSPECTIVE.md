# 회고 — 손글씨 OCR PoC (2026-08-13)

하루 동안 엔진 3종을 재고, 입력 형태와 호출 구조를 바꿔가며 CER 0.189 → 0.037 까지 내렸다.
아래는 결과보다 **틀렸던 과정**에 대한 기록이다.

## 무엇을 했나

| 단계 | 결과 |
|------|------|
| 합성 샘플로 파이프라인 구축 | CER 0.029, 일치율 83% |
| 실제 손글씨로 재측정 | **CER 0.222, 일치율 0%** — 합성이 실제를 심하게 과대평가 |
| 엔진 교체 (surya) | 0.222 → 0.070 |
| 엔진 교체 (CLOVA) | 0.070 → **0.037** |
| 표본 확대 | 필자 1명 11행 → **5명 53행** |
| 입력 단위 실험 | CLOVA 는 페이지 통째가 2배 정확 |
| 호출 최적화 | 여백 제거 + 50% 축소 병합으로 **5회 → 1회** |

## 세 번 같은 실수를 했다 — 표본 하나로 일반화

오늘 가장 반복된 실패는 기술적인 것이 아니라 **판단 습관**이었다.

| # | 성급한 결론 | 실제 |
|---|-------------|------|
| 1 | "따옴표를 정답에서 뺐으니 surya 는 과소평가됐다. 실제 성능은 더 좋다" | **반대였다.** 포함하니 같은 필체 CER 이 0.092 → 0.157 로 **올랐다**. surya 는 따옴표를 인식하되 종류를 혼동했다 |
| 2 | "병합이 실패했으니 페이지 5회가 최소이자 최적이다" | **틀렸다.** 실패 원인은 크기(8000px 제한)였고, 축소하니 **1회로 되면서 정확도도 유지**됐다 |
| 3 | "여백 제거가 단일 페이지에 불리하다"(002 기준) | **착시였다.** 5장으로 늘리니 나머지 4장은 ±0.005 이내, 방향도 2:2 |

세 번 모두 **관측 1건을 근거로 일반화**했다.
1번은 추론만으로 단정했고, 2번은 실패 1건을 원인 규명 없이 결론으로 옮겼고, 3번은 표본 1장을 믿었다.

**배운 것**: "이렇게 보인다"와 "이렇다"를 구분해야 한다.
표본이 1이면 결론이 아니라 **가설**로 적어야 한다.

## 사용자 지적이 방향을 바꾼 지점

위 실수 중 2·3번은 스스로 발견하지 못했다. 지적을 받고서야 재측정했다.

| 지적 | 결과 |
|------|------|
| "훼손 정도에 따른 결과값을 보는 게 좋지 않을까?" | 축소율 곡선을 재면서 **API 제한이 바이트가 아니라 8000px** 임을 발견 |
| "빈 공간을 잘라내고 병합하면 훼손이 덜하지 않을까?" | 병합 높이 51% 감소 → **병합 성공의 결정적 열쇠** |
| "PaddleOCR 은 리눅스에서 하면 안 될까?" | 내 기각 사유가 **Windows 한정**이었음을 확인. 측정은 전부 Linux 에서 하고 있었다 |

공통점은 **내가 "안 된다"고 판정한 것을 다시 열어본 것**이다.
실패를 원인별로 분해하지 않고 결론으로 직행한 것이 문제였다.

## 잘 작동한 것

**1. 셀프체크가 잘못된 가정을 잡았다**
CER 계산에서 `cer("가 나", "가나") == 0.0` 이라고 단언했다가 테스트가 실패했다.
코드가 아니라 **내 단언이 틀렸다**. TOPIK 은 띄어쓰기도 채점하므로 CER 에 공백을 포함하는 것이 맞았다.

**2. 상한을 먼저 재서 헛수고를 막았다**
"띄어쓰기 교정 후처리"를 만들려다, 먼저 얻을 수 있는 상한을 쟀다.
→ 32% 였고, 이는 **이미 보고 중인 지표와 같은 값**이었다. 새 코드로 얻을 것이 없어 만들지 않았다.
만들었다면 하루를 썼을 것이다.

**3. 오류를 유형화해 막다른 길을 확인했다**
가장 잦은 혼동(`ㅊ→ㅈ`)은 오류 행의 28% 에 등장한다. 고칠 가치가 있어 보였다.
그러나 **그것만 고쳐 완전 복구되는 행은 0건**이었다 — 행당 오류가 평균 1.7건씩 겹쳐 있었다.
후처리로 일치율을 올리는 길이 막혔다는 것을 **숫자로** 확인했다.

## 실패한 것

**PaddleOCR: 3회 시도, 3회 실패**
설치와 API 확인까지는 성공했으나 추론에서 죽었다(Segfault → oneDNN 비호환 → 플래그 무효).
정확도는 **재보지도 못했다.**
"부정확하다"가 아니라 "실행하지 못했다"로 기록했다. 이 구분은 중요하다.

## 다음에 다르게 할 것

1. **표본 1개는 가설로 적는다.** 결론으로 쓰려면 최소 3~5개.
2. **실패는 원인별로 분해한 뒤 결론짓는다.** "실패했다" → "무엇이 한계였나" → "그 한계를 피하면?"
3. **오류 응답 본문을 버리지 않는다.** 400 의 원인을 못 본 채 추측하느라 시간을 썼다.
   본문을 출력하자 `images=size must be between 0 and 1` 한 줄로 끝났다.
4. **새 기능을 만들기 전에 상한을 먼저 잰다.** 띄어쓰기 교정에서 통했던 방법이다.

## 남은 것

기술 리스크는 대부분 해소됐다. 남은 것은 성격이 다르다.

- **사용성**: 초안 교정이 백지 타이핑보다 빠른가 (제품 방향 자체의 검증)
- **일반화**: 타겟인 외국인 학습자 필체에서도 재현되는가 (현재 표본은 전원 한국인)
- **정책**: 최고 성능 모델이 클라우드라 필적이 외부로 나간다 — 법무·정책 결정

상세는 [NEXT_STEPS.md](NEXT_STEPS.md) 참조.
