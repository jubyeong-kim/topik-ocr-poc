# 문제 정의서 — TOPIK 손글씨 답안 자동 인식

## 1. 도메인 설명

**한국어 교육 / TOPIK II 쓰기 영역 학습**

TOPIK(한국어능력시험) II 쓰기는 51~54번 4문항으로 구성되며, **실제 시험은 답안지에 손으로 직접 쓰는 방식**이다.
따라서 수험생 대부분은 평소 연습도 종이에 손으로 한다.

필자는 직전 프로젝트로 TOPIK 쓰기 연습 웹앱([TOPIK Write](https://topic-write.onrender.com))을 만들어
답안을 입력하면 규칙 기반으로 문법·스타일 오류를 자동 채점하는 기능을 구현했다.
이 PoC는 그 과정에서 직접 마주친 불편을 개선 대상으로 삼는다.

## 2. 현재의 문제

종이에 손으로 연습한 답안을 채점받으려면 **앱에 처음부터 다시 타이핑**해야 한다.

| 단계 | 현재 방식 | 문제 |
|------|-----------|------|
| 1. 답안 작성 | 종이에 손으로 씀 (실제 시험과 동일) | — |
| 2. **디지털화** | **앱에 재타이핑** | **순수 낭비 시간. 54번은 600~700자** |
| 3. 채점 | 자동 채점 | — |

- 학습자 입장: 재타이핑이 번거로워 앱을 안 쓰게 되거나, 처음부터 타이핑으로 연습 → **실제 시험 환경과 괴리**
- 교사 입장: 학생 답안지를 일일이 눈으로 읽고 손으로 첨삭 → 학생 수에 비례해 시간 증가

## 3. 개선 가설

> 2단계(디지털화)에 OCR을 넣으면, **답안지를 촬영하는 것만으로** 기존 채점 파이프라인에 그대로 연결된다.

```
[현재]  종이 답안 → 사람이 재타이핑 → 채점 엔진 → 피드백
[개선]  종이 답안 → 📷 촬영 → OCR → 채점 엔진 → 피드백
                            └─ AI가 대체하는 구간
```

핵심 가정: OCR 텍스트가 완벽하지 않아도, **채점 결과가 정타 입력과 같으면 실용적으로 충분하다.**
(글자 단위 100% 정확도보다 최종 채점 일치율이 실제 효용에 가깝다)

## 4. 대상 사용자

| 사용자 | 얻는 이점 |
|--------|-----------|
| **TOPIK 수험생** (1차) | 실제 시험처럼 손으로 연습하면서도 즉시 자동 채점 |
| **한국어 교사** | 답안지 일괄 촬영 → 초벌 첨삭 자동화, 교사는 검토에 집중 |
| **필자 본인** | 기존 앱의 입력 장벽 제거 |

## 5. 성공 기준

| 지표 | 기존 방식 | 목표 | 측정 방법 |
|------|-----------|------|-----------|
| **디지털화 시간** (51/52번 한 문장) | 재타이핑 약 20초 | **5초 이하** | 촬영~텍스트 출력까지 실측 |
| **디지털화 시간** (54번 700자) | 재타이핑 약 8분 | **30초 이하** | 동일 |
| **문자 오류율(CER)** | 0 (사람이 정확히 옮김) | **0.15 이하** | 정답 대비 레벤슈타인 거리 |
| **공백 무시 일치율** | 100% | **80% 이상** | 12건 중 일치 건수 |

**최종 성공 판정**: OCR 결과로 채점한 점수가 정타 입력 채점 점수와 **동일한 비율이 80% 이상**이면
"재타이핑을 대체할 수 있다"고 본다.

## 6. 모델 선정 근거

손글씨 한국어를 텍스트로 바꾸는 것이 핵심이므로 **OCR 계열**로 후보를 좁혔다.

| 후보 | 판단 | 이유 |
|------|------|------|
| **EasyOCR** ✅ **채택** | 채택 | 한국어 사전학습 모델 제공, `pip` 단독 설치(Python 3.14 휠 존재), GPU 불필요, **완전 오프라인** → 학습자 필적이 외부로 나가지 않음(프라이버시), 재현성 확보 |
| Tesseract | 기각 | 한글 지원은 되나 **손글씨에 특히 취약**, 별도 바이너리 설치 필요해 재현성 저하 |
| PaddleOCR | 기각 | 성능은 경쟁력 있으나 Windows 환경에서 `paddlepaddle` 설치가 까다로워 "누구나 실행" 조건에 불리 |
| 클라우드 OCR API<br>(**네이버 CLOVA OCR**) | **재검토 → 측정함** | 최초에는 "비용 + 답안 이미지 외부 전송"을 이유로 기각했으나, **로컬 모델의 성능을 평가할 기준선이 없다**는 문제가 있었다.<br>→ **PoC 단계에서는 정확도 상한 확인용으로 측정**하고, **상용화 시에는** 필적(개인정보) 외부 전송 문제로 온프레미스 모델이 필요하다는 조건을 명시하는 쪽으로 방침을 바꿨다. 측정 결과는 [README](README.md#-핵심-결과--실제-손글씨-53행--필체-5종-엔진-3종-비교) 참조 |
| 비전 LLM | **보류(향후)** | 손글씨 인식률이 가장 높을 것으로 예상되나 API 키·비용이 필요해 PoC 재현성을 해침. 한계 극복 단계에서 비교 예정 |
| YOLO / SAM | 기각 | 답안지는 **레이아웃이 단순한 단일 텍스트 영역**이라 객체탐지·세그멘테이션이 기여하는 바가 없음. 불필요한 복잡도 |

## 7. 데이터

실제 손글씨 사진을 아직 확보하지 못해, **합성 샘플 생성기**를 함께 구현했다.

- 한글 폰트(궁서·궁서체·바탕·굴림·맑은고딕)로 답안 문장을 렌더링
- 촬영 열화를 흉내: 미세 회전(±1.8°), 가우시안 블러, 노이즈 합성
- **시드 고정** → 누가 실행해도 동일한 12건 샘플과 정답 라벨이 생성됨 (재현성)

⚠️ **한계 명시**: 합성 샘플은 폰트 기반이라 **실제 육필보다 인식이 쉽다.**
따라서 이 결과는 성능의 **상한선(upper bound)** 으로 해석해야 하며,
실제 손글씨 사진 확보 후 `data/real/` 에 넣고 재측정하는 것이 다음 단계다.

> ✅ **이 가정은 실측으로 확인되었다.** 공개 손글씨 데이터셋(Nexdata 데모)으로 재측정한 결과
> 합성 83% → 실제 0% 로 성능이 붕괴했다. 상세는 [README](README.md#b-실제-손글씨-11건--목표-미달-가설-기각) 참조.
